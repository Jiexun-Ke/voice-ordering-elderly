# Core service — works on any platform
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
soundfile>=0.12
numpy>=1.24
rapidfuzz>=3.9

# Evaluation
jiwer>=3.0

# Fallback ASR engine (CPU, runs anywhere: Linux / Windows / Intel or Apple Mac)
faster-whisper>=1.0

# Primary ASR engine (Apple Silicon ONLY — pip install will fail on other platforms).
# Install separately on a Mac:
#     pip install mlx-qwen3-asr
# Optional second engine for the Day 4 dialect probe:
#     pip install mlx-meralion

# Test-only. starlette's TestClient needs an HTTP client, and which one it
# wants depends on the starlette version pip resolves for your Python:
# newer starlette wants httpx2, the versions that support 3.9 want httpx.
# Installing both is harmless and avoids a confusing collection error.
httpx
httpx2; python_version >= '3.10'
pytest>=8.0
